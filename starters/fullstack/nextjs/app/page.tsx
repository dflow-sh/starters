export const dynamic = "force-dynamic";

export default function Home() {
  return (
    <main>
      <h1>dFlow Next.js starter</h1>
      <p>
        This page is rendered on the server by default (App Router). Use{" "}
        <code>npm run dev</code> locally or <code>npm run build</code> then{" "}
        <code>npm run start</code> for production.
      </p>
    </main>
  );
}
